import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
  root: { width: '100%' },
  input: { padding: '5.5px 4px !important' },
  body: { display: 'flex', flexDirection: 'row' }
})

export default useStyles
