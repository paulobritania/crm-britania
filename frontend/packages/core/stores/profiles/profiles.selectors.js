export const selectMicroViewList = (state) => state.profiles.microViewList

export const selectViews = (state) => state.profiles.views

export const getAllProfiles = (state) => state.profiles.profiles
