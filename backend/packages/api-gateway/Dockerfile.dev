FROM node:16-alpine

WORKDIR /usr/src/app

COPY package*.json tsconfig*.json ./

RUN npm install

RUN npm i -g @nestjs/cli

COPY src/ src/

RUN npm run build

# ENTRYPOINT node --inspect=0.0.0.0 main.ts

CMD ["npm", "run", "start:debug"]

