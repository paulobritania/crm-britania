{
  "configurations": [
      {
          "type": "node",
          "request": "attach",
          "name": "Docker: Attach to Node",
          "remoteRoot": "${workspaceFolder}/usr/src/app",
          "localRoot": "${workspaceFolder}/backend/api-gateway/src"
      }
  ]
}